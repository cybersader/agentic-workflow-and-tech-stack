---
title: 2026-09-13 — Worklog
description: Significant scaffold changes on 2026-09-13.
stratum: 5
status: log
date: 2026-09-13
tags:
  - log
  - worklog
  - meta
---

## Desktop computer use: bridge build, independent review, and repair (waves 2–3)

Continues the 2026-09-12 entry. Nothing downloaded, installed, or registered; no commit yet.

- Wave 2 (Opus builder, new tree `profiles/desktop-bridge/`, Node ESM, zero deps, mirrors `profiles/browser-bridge/`): `desktop-bridge-request` (prepare only), `desktop-bridge mcp` stdio proxy as the authority, `stop-desktop-bridge` (`/proc`-validated group kill), `hooks/desktop-mcp-guard.py` PreToolUse backstop on `mcp__computer-use-linux__.*`, `install-desktop-bridge.sh` (`PINNED_SHA256=""` blocks any real install). Bridge policy is strictly narrower than the core: 15 min / 20 actions / 30 observations / 4 allowlist entries, `anyWindow` refused, no keyboard or clipboard class.
- Wave 3 (Opus, read-only, fresh context): ship-after-fixes. All eight frozen decisions held. Findings kept at `~/.claude/scratch/desktop-bridge/wave3-review.md` (C1, H1–H3, M1–M8, L1–L9). The three that mattered: **C1** tool arguments were forwarded verbatim, so with the allowlisted window focused a `click` at foreign coordinates or a `get_app_state` naming another app passed the gate; **H1** scope denials named the focused app and cost nothing — a free activity oracle; **H2** a logical-coordinate crop rect on a device-pixel capture cropped the wrong region (reproduced), and the README claimed otherwise. **H3** decompression bomb via `inflateSync` without `maxOutputLength` (reproduced, +810 MiB RSS).
- Human direction resolved the one open design question: the goal is *usable* computer use, not a screenshot-only demo. So the pilot stays `["screenshot","pointer"]` and C1 is closed by per-operation argument validation (`lib/argument-policy.mjs`: classified keys only, pointer points/offsets/extents bounded by the verified focused-window geometry, selectors must equal the verified window, text-class keys refuse, unclassified keys refuse) rather than by narrowing the grant.
- Repair (two Opus workers, disjoint write roots). Core: malformed terms now *deny* instead of raising (`unsupported_version` for non-mapping terms, `scope_denied` otherwise — no new code, keeps JS/Python parity), `anyWindow` widens only on boolean `True`, `_window_match` and the schema pattern both reject U+007F, one fixed renderer sentence disclosing the uncharged focus lookup (J2). Suite 350 → 365. Bridge: C1 as above; H1 generic denial text plus one observation charged per denial that consumed a lookup, with a latch that closes lookups once the ledger cannot charge; H2 `reconcileCaptureScale` (scale never inferred — capture dims must equal screen dims × an explicitly reported scale, else refuse); H3 IDAT byte cap + `maxOutputLength`; M3 handle hygiene (`O_NOFOLLOW`, fstat uid/mode, skip-with-cap, prune-on-write, delete-on-stop); M4 replay refusal + failure caching; M6 16/64 MiB frame lanes with newline resync; M7 injectable runtime root (tests no longer write into the live runtime; 57 stray empty `runtime/runs/dsk-*` dirs removed); M8 installer refuses to reinstall over a live session. Suite 58 → 127.
- Builder judgment calls flagged for the re-review rather than accepted on the builder's word: `button` accepted as a bounded enum; denial charge applies to *any* post-lookup refusal; handles deleted on stop, not on grant resolution (so a proxy restart can relocate a live grant); shutdown now signals the group. `move_window`/`resize_window` are near-unusable under the conservative geometry rule and are not in the pilot.
- Design doc corrected to match the code: the child owns the portal handles; the bridge owns the child's process group; killing the validated group is what closes the portal session (§3 step 4, the authority bullet, and the cleanup paragraph).
- Wave 4 re-review of the repairs (Opus, read-only, fresh context; findings at `~/.claude/scratch/desktop-bridge/wave4-rereview.md`): C1 confirmed closed under 89 adversarial argument probes (numeric strings, NaN/Infinity/1e300, prototype keys, 32 alias spellings, nested objects); H2, M4, M6, M7, M8, parity all closed; all eight frozen decisions hold. Five new mediums, all introduced or left by the repair: two residual free oracles around the focus lookup (reused request id replayed the denial charge; `WINDOW_UNKNOWN` inside the lookup uncharged), binding-handle cap selected by filename not age, PNG decode ran before the screen-size check (260 KB → ~512 MiB peak), and the approval card still said the lookup was free while the bridge charged one observation per refusal.
- Decision on the card (N5): keep the charge, make the card honest. The charge is what closes the activity-tracker hole; the card is what the human reads at approval time. New fixed sentence: "A step that is allowed costs only its own action or observation; a step the bridge refuses after that check costs one observation." Contract §4.3 gained "What a step costs".
- Second repair (two Opus workers): denials cached by fingerprint like failures; `WINDOW_UNKNOWN` charged; handles ordered by `createdEpoch` with junk pruned by name pattern + mtime; IHDR reconciled against verified screen × explicit scale *before* any IDAT is read (MAX_PIXELS now 33 Mpx = 7680×4320); per-frame stdio measurement; `move_window`/`resize_window` refuse as `OPERATION_POLICY_UNDESIGNED` rather than carry a wrong geometry rule. Suite 127 → 139, each new test proven by reverting its mechanism. Delta re-review: ship-after-fixes with four lows/one availability-only medium (junk flood of the handle directory can hide a live grant — no grant widened) → third micro-fix, plus `MAX_SCALE` raised to 3 (Plasma's slider max) decoupled from the pixel ceiling. Host displays checked read-only: all three at scale 1.
- Third micro-fix (one Opus worker): future-dated handles (`createdEpoch` > now + 300 s) skipped and pruned like junk; a scan that hits the 256 bound refuses as `BINDINGS_FLOODED` naming the directory rather than resolving whatever survived; every focus-lookup failure except the latched backend fall-through now charges and caches like a denial (a lookup timeout is no longer a free probe); `window-facts` shares `MAX_SCALE` (3) with the PNG path and `MAX_PIXELS` (33 Mpx) stands alone. Suite 139 → 143. Delta re-review: five of six closed; the skew rule alone did not stop crowd-out — 32+ ordinary handle requests filed *after* an approval pushed the approved handle out of the 32-record slice with no forgery at all (N14, availability-only). Fourth micro-fix: `recordCeilingHit` reported beside `scanBoundHit` and routed to the same `BINDINGS_FLOODED` refusal, so the human is told to prune the directory instead of "no active grant". Suite 144, `--check-source` exit 0 with `PINNED_SHA256` still empty. Final bridge diff sha256 `3f0f868e…6362`, 46 files. Build is done; next step is the separately authorized binary download and §5a.
- Recorder note: the repair-pair and review runs would not close because the recorder's journal never received those workers' stop events (`agentTranscriptPath: null`); the work is durable here and in the scratch review files. Worth a look at the SubagentStop hook path later.
- Still untested against the real binary, by design: `focused_window`'s actual response shape, how screen/scale facts are reported, and every argument spelling in the classification table. §5a (second user's Plasma session) is where those get calibrated, after a separately authorized download with a pinned digest.

## Desktop computer use: download, install, §5a smoke test (same day, separately authorized)

- Download authorized by the human: v0.5.0 `computer-use-linux-x86_64-unknown-linux-gnu`, digest cross-checked against the release `.sha256` asset and pinned in `install-desktop-bridge.sh` (`d21e7983…020e`). Install authorized: binary to `~/.local/bin/computer-use-linux`, bridge launchers to `~/.local/bin/`, hook merged into `~/.claude/settings.json` (backup kept), proxy registered project-scoped in `.mcp.json` (untracked; decide at commit time).
- The binary has no `--version`/`--help`; `screenshot --help` starts a capture. `doctor` reported ready (input `abs_pointer, ydotool, portal`; screenshot `portal`; window control `kwin`; accessibility `at_spi`).
- First screenshot attempts hung. Not the frontend (`org.freedesktop.portal.Desktop` and the KDE impl both answered property reads) and not the human's other running project: it was the KDE "Allow Apps to Take Screenshots?" portal dialog queued once per attempt. After "allow and remember" every capture returned. Lesson for §5b: the first governed capture on a fresh host needs the human at the screen.
- §5a passed end to end through the raw binary: untargeted capture is the multi-monitor union (4000×3440) auto-downscaled to `max_bytes` 2 MiB (`scale` 0.48); targeted capture by `pid` is window-cropped (`cropped_to_window: true`, `scale: 1.0`); a window-relative click (`relative: true`) and an absolute click both pressed the OK button of a kdialog scratch window (process exited rc=0). Real `tools/list`, `focused_window` and `list_windows` shapes captured in `~/.claude/scratch/desktop-bridge/5a-calibration-facts.md`.
- Calibration findings that invalidate build-time assumptions: selectors are `pid|app_id|wm_class|title|window_id|tty|terminal_*` (`window`/`windowClass`/`wmClass` never existed); `window_id` is a u64 past 2^53, so Node `JSON.parse` corrupts it (refuse it, never compare it); `title` can be `null`; `scale` is a downscale factor in (0,1], not a display scale; `click` carries `relative` plus accessibility matchers; `screenshot` carries `raise_window` (a focus side effect on an observation) and `full_screen`; `structuredContent` is `null` on some responses; `run_shell` is absent from `tools/list` unless the env var is set; `invoke-action` is really `perform_action`. Frozen decisions for the calibration worker: target-or-refuse screenshots (inject verified `pid`, `raise_window:false`, verify `cropped_to_window` and the metadata arithmetic) replace crop-or-refuse; accessibility matchers, terminal selectors and `window_id` refused in the pilot; `relative:true` requires a selector.
- Human-factor note: two scratch dialogs "auto-closed" during the test because the human, still at the screen from the portal prompts, was clicking them. Confirmed by a yes/no probe (20 s) and a konsole `read` probe (no synthetic key). Next time say up front which windows the test owns.

## Desktop computer use: calibration to the real binary, attestation, and workflow baking

- Calibration (one Opus worker, write root `profiles/desktop-bridge/`, frozen input `~/.claude/scratch/desktop-bridge/5a-calibration-facts.md`): `window-facts` reads the real `focused_window` envelope and refuses `window_id` outright; `argument-policy` carries the real tables (snake_case selectors, `relative` switch, four-coordinate `drag`, `scroll` direction/pages, capture tuning bounds; AT-SPI matchers and terminal selectors refused); the crop path and `MAX_SCALE` are gone — `screenshot` now injects the verified `pid`, pins `raise_window:false`, and verifies `cropped_to_window`, `window_title`, `coordinate_*` and `width == round(coordinate × scale) ± 1` before forwarding the image unchanged; `list_windows`/`list_apps` refused by name (`OPERATION_NOT_IN_PILOT`). New `test/fixtures/calibration.mjs` keeps captures as raw JSON text so the u64 survives. Suite 144 → 165. Caveat: `node --test test/` fails on Node 22.23.1; the command is `node --test --test-concurrency=1 test/*.test.mjs`.
- The calibration worker found the fall-through latch would fire at startup on this host (`doctor` lists `ydotool` as available; the primary pointer path is itself uinput). Orchestrator decision, reversible: replace fall-through detection with **input-backend attestation** (decision 9): `doctor` must list `abs_pointer` under `capabilities.input` or the session latches `BACKEND_UNATTESTED` before it opens (uncharged, never returned); every action result (`stepCounter === 'actions'`) must be `ok:true` with the `uinput absolute pointer` message or the step refuses, charges, caches and latches. Observations are not attested. `capabilities.preferred.input` deliberately not used (a preference, not a record) and pinned by a test. Decision 10: the proxy never passes the child's `initialize.instructions` through (app-controlled text written for the ungated binary); `lib/instructions.mjs` renders its own from the live grant (2,043 chars; a grant without `click` does not advertise `click`). The old `DOCTOR_REPORT` fixture turned out to be invented (`backends.input`) — replaced with the redacted real capture. Suite 165 → 173, three mechanisms proven by revert; `--check-source` exit 0, digest `c4fcd6d1…ae1d`. Design doc: decision 3's fall-through clause struck in place; decisions 9–10 appended. Follow-up done directly: `lib/instructions.mjs` added to the installer's `required_source_files`.
- Still unverified: `abs_pointer` is attested, not pinned (no upstream switch excludes a backend — containment stays the argument gate's job); `drag`/`scroll` echoes never read from the real server; `get_app_state` shape; HiDPI; cross-session portal re-prompting.
- Workflow baking (one Opus worker; in hindsight a bookkeeping-sized task): new `02-stack/01-ai-coding/desktop-computer-use-pilot.md` (status `research`), index entry, global rule-7 sentence making `desktop_session` the third permission kind, root `CLAUDE.md` routing row. Installed `~/.claude/CLAUDE.md` still needs `bash install.sh --claude` after commit. Open decision recorded in the doc: project-scope `.mcp.json` vs how other projects reach the bridge.
- Commit plan: `~/.claude/scratch/desktop-bridge/commit-desktop-work.sh` (three commits; `index.md` and the global `CLAUDE.md` carry unrelated in-flight hunks, so only their desktop lines are staged via `git apply --cached --unidiff-zero` patches). Human runs it.
- Worker-model preference restated by the human mid-session and saved to memory: Opus 5 or Sol 5.6 for most delegated work; Sonnet/Haiku only for search and gathering.
- Registration scope decided and executed: the proxy is now registered at **user** scope (`claude mcp add --scope user computer-use-linux -- ~/.local/bin/desktop-bridge mcp`), and the committed `.mcp.json` (from `2b0a7bd`) was removed. "Never `--scope user`" had assumed registration was itself a capability. Probe evidence from an unrelated cwd (`~`): a grantless `desktop-bridge mcp` spawns no `computer-use-linux` child, opens no portal session, answers `initialize` with `serverInfo.version` `desktop-bridge/0.1.0` and its own refusal instructions ("no approved desktop_session grant is active …"), returns `tools: []`, and leaves no raw-binary process behind; a grant is additionally workspace-bound (context derived from the canonical cwd; `resolveSingleGrant` skips `CONTEXT_MISMATCH`), and the `PreToolUse` guard is already global in `~/.claude/settings.json`. So the approved card is the control and registration is plumbing — cost is one idle Node process and a refusal-only tool group per session, benefit is that every current and future workspace can prepare a request with no per-project `.mcp.json` and no extra MCP-approval prompt. Raw binary still registered nowhere. Caveat recorded in the docs: the proxy resolves its grant at startup, so after approval the session must reconnect the server (`/mcp` → reconnect) or start fresh. Portagenty's `pa init --with-agent-hooks` was deliberately **not** touched — user scope already covers current and future workspaces, so there is nothing for a per-project initializer to add. Docs updated: `profiles/desktop-bridge/README.md`, `02-stack/01-ai-coding/desktop-computer-use-pilot.md` (open decision closed), design doc §6 line struck in place with decision 11 appended.
- Audit of that workspace-binding claim found the leg unenforced: handles all land in one per-user `runtime/bindings` directory and `resolveSingleGrant` replayed each handle's *stored* context to the core, so `CONTEXT_MISMATCH` compared a value against itself and a grant approved in one project resolved for a proxy started in another under the same UID. Enforced in the proxy instead (`lib/session.mjs`): before the core is asked, a handle is eligible only if its recorded uid matches and the proxy's own `realpath(cwd)` is the recorded working directory or a path-segment ancestor of it; ineligible handles are skipped like core-refused ones — never claimed, not counted toward the exactly-one-grant rule, refused under the existing `NO_ACTIVE_GRANT` text with no other workspace's path leaked. Workspace root is injectable for tests. Six tests added (match, ancestor, sibling, string-prefix-not-ancestor, uid mismatch, missing fingerprint), each verified to fail with the check disabled; suite 173 → 179. Still per-UID: a cwd is a path fact, not an identity. Recorded as design decision 12.
