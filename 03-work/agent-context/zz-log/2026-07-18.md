---
title: 2026-07-18 — Worklog
description: Vault-knowledge-engine landscape resweep — 3 bounded Sol research agents (MegaMem/Graphiti state, vault-KG field survey, Obsidian-as-agent-surface) into a new zz-research note. Verdicts — MegaMem fails the rebuildable-from-markdown filter (graph-only memories, non-deterministic re-extraction); QMD is the new ck challenger (first-class MCP + local GGUF stack); Claudian (~1.4M downloads) makes "agent inside Obsidian" an adopt-not-build decision; official Obsidian CLI (1.12) is the agent-primitive to watch. MegaMem memory crumb updated to watchlist.
stratum: 5
status: log
date: 2026-07-18
tags:
  - log
  - worklog
  - research
  - search
  - knowledge-graph
  - obsidian
  - mcp
---

## Session: vault knowledge engine — landscape resweep

The "Obsidian as my Work IQ" thread got its structured pass. New note: `zz-research/2026-07-18-vault-knowledge-engine-landscape.md` — vision statement, the **rebuildable-from-markdown hard filter**, a 5-layer stack (vault → lexical/semantic/structural-graph/temporal-KG indexes → MCP → harness → surface), and findings from **3 bounded research agents** (Sol lane, research-only, no sub-spawning — well under the disclosure threshold; user pre-approved "landscape resweep, not too many agents").

Key verdicts (receipts + sources in the research note):

- **MegaMem: UNPROVEN → FAIL** on the filter. v1.7.5, active, but graph-only memory tools, non-deterministic LLM re-extraction, and no documented rebuild procedure mean the graph DB is load-bearing state. Demoted to watchlist; memory crumb `reference_megamem.md` rewritten accordingly. Graphiti itself is healthy (FalkorDB Lite = embedded backend now) but ingest still meters LLM calls per note-change.
- **QMD (tobi/qmd) is the ck challenger** — since the 2026-05-12 alternatives note it gained first-class MCP (stdio + Streamable HTTP daemon), all-local GGUF embed/rerank, single rebuildable SQLite index, ~28k stars. Eval per the existing axes is next-action #2.
- **"Claude Code inside Obsidian" is now an established category** — Claudian at ~1.4M downloads runs the full loop (Bash, diffs, MCP, subagents); ACP is the emerging neutral protocol; Copilot v4 pivoting to wrap Claude Code/Codex/OpenCode. Conclusion: **adopt, don't build** the desktop panel; the house differentiator stays OpenClast's sovereign remote/browser gateway (Challenge 39/43).
- **Official Obsidian CLI (1.12, 2026-02)** is the big platform primitive: agent-aimed note CRUD/search/backlinks/Bases/dev-tools against the live app. Already wrapped by our `obs-*` helpers; ZethicTech/obsidian-mcp wraps it as typed MCP tools.
- **Security posture:** Obsidian plugins are unsandboxed by official admission — plugin approval dialogs are courtesy UI. Trust boundary for agent-drives-vault work must live outside Obsidian (harness permission layer or OpenClast gateway).

Next actions logged in the note: (1) trial Claudian on a non-critical vault, (2) QMD-vs-ck side-by-side, (3) prototype derived link/frontmatter graph as tier-2c, (4) re-check MegaMem/Graphiti in ~a quarter on named triggers.

## Session (cont.): distill resweep → 02-stack pattern docs

User asked for a workflow to distill the resweep into the KB with intuitive concept-building diagrams. Ran a bounded 4-agent workflow (3 writers + 1 verifier, Sol lane, ~417k subagent tokens — disclosed, under threshold). New docs in `02-stack/04-knowledge-mgmt/`, all `status: research` (new-promotion gate), pairwise cross-linked, registered in the section index:

- `retrieval-concepts-from-grep-to-knowledge-graphs.md` — concept-builder: lexical → semantic → structural graph → temporal KG from intuition up; RAG vs agentic retrieval; decision table. (~3.7k words)
- `vault-knowledge-engine-architecture.md` — the pattern: rebuildable-from-markdown as the survival trait + delete-the-DB test; 5-layer stack; adoption path; trust-boundary-outside-the-editor. (~2.4k words)
- `vault-search-and-memory-landscape-2026-07.md` — date-stamped field snapshot by filter verdict; MegaMem non-rebuildable-state data-flow; agent-surface options. (~2.7k words)

11 Mermaid diagrams total, all parsed; frontmatter checker clean; tier-2 audit clean. Verifier fixed 2 YAML descriptions (unquoted colons) and 2 phantom links. These are stratum-2 *promotions with evidence* (primary-source resweep behind them in zz-research) but start `status: research` per the knowledge-curator gate — promote to `active`/`stable` only after second use.

## Session (cont.): backlog review + public-sync unblocked

Working-tree backlog cleared in 4 commits (all reviewed before commit):

- `f9d6953` — tailscale file-share pattern four→six flavors (Windows Taildrop reality check, GH #14393, Drive/Filebrowser flavors) + cross-device index links.
- `38c5271` — `agent-launcher.sh` (ccx/x) + the u-variant `unalias` re-source guard, with the 07-17 zz-log addendum.
- `3d73c62` — **the important one: public sync had been failing on EVERY push since ≥07-11.** The strict leak gate was correctly catching PII in two agent-context files that the agentic allowlist ships: `zz-research/2026-07-17-kb-audit-findings.md` (50× personal repo path inside audit-verifier JSON `reason` fields) and `zz-log/2026-05-10.md` (tailnet hostname + AppData paths in the Dokploy webhook recipe). Sanitized to `<repo>/`, `<your-tailnet>`, `<you>` placeholders; also committed the accumulated May–July site copies of agent-context. **Post-push: sync-public green for the first time in a week** — the vault-knowledge-engine docs and the whole backlog are now live on the public mirror.
- Reverted an uncommitted `/docs` base-path rewrite in `site/src/content/docs/index.mdx`/`index.trunk.mdx` (leftover from a `SITE_BASE=/docs` Dokploy build run of `rewrite-base.mjs` — machine-generated, wrong for the committed default; `rewrite-base` regenerates it at build time when needed).

Lesson for the leak-gate pattern: agent-generated audit artifacts (verifier JSON with absolute `file` paths) are a systematic PII source — any workflow that writes agent output into `agent-context/` should placeholder repo-absolute paths at write time. The gate did its job; it just had nobody watching the failures.

## Session (cont.): IR-daemon project — Gates 0-1 + handoff

The Omnisearch-escape thread became a real project. Two more bounded research agents (engine landscape + vault-ingest glue; Sol lane, 5 total today): Meilisearch/Typesense still own app-search but personal-RAG now builds embedded (SQLite FTS5+sqlite-vec, LanceDB); NO mature Obsidian↔engine glue exists anywhere ("everyone writes ~200 lines" confirmed; markdown-vault-mcp is the feature-complete behavioral blueprint). Gate 0 (owner picks): embedded Rust daemon (Tantivy + vectors + ONNX), standalone repo, v1 = Linux daemon/CLI/MCP/dumb plugin. Gate 1: contract draft v0 written (`zz-research/2026-07-18-ir-daemon-contract-draft.md`) and APPROVED with two owner amendments (lite tier = enterprise-deployment tier, not just fallback; semantic-lite = possible-but-expensive experiment per Smart Connections precedent). Handoff for the new workspace: `agent-context/handoffs/2026-07-18-ir-daemon-project-handoff.md` — owner scaffolds via Portagenty, brings the project back for cross-referencing later. OpenClast reply-handoff (gateway requirements vs contract) queued in the handoff, not fired.
