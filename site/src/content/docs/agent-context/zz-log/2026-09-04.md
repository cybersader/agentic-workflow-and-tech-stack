---
title: 2026-09-04 — Worklog
description: Significant scaffold changes on 2026-09-04.
stratum: 5
status: log
date: 2026-09-04
tags:
  - log
  - worklog
  - meta
---

## Repeatable launcher-state migration

- A long-lived shell rewrote Cynario's saved `x` choice in the legacy format after an earlier cross-provider migration had already created a different rollback backup. The existing overwrite guard preserved evidence but blocked every later launch.
- Made migration recover safely from this stale-shell race: archive the prior active backup in private content-addressed history, promote the latest valid legacy choice to the active rollback point, and atomically write the versioned state. Symlinks, foreign ownership, malformed backups, non-private history, and content-address collisions still fail closed.
- Corrected direct rollback's accidental dependence on a caller-scoped Bash variable and added regression coverage for repeated collisions, deduplication, rollback selection, dry-run behavior, and hostile filesystem objects.

## Astra and Fable cross-provider activation

- Added explicit GPT-6 Astra launcher modes without changing the default for new directories. Corrected `astrax` to the OpenAI-only naming contract: Astra orchestrates, delegated work is guard-locked to GPT-5.6-Sol, both use the weekly Codex pool, and only Codex is preflighted. `astraxm` leaves both worker controls unset and exposes direct Sol, Sonnet, Opus, and Fable leaf workers. Both Astra modes retain containment, bounded retries, and the 272,000-token profile unless the caller supplies `--settings`.
- Activated exact `claude-fable-5-1` proxy routing. `fablex` guard-locks workers to GPT-5.6-Sol, while `fablexm` exposes direct Sol, Sonnet, and Astra workers without a default or lock. Neither lane silently falls back to Fable 5.
- Added a deterministic one-use `approve-agent <nonce>` protocol for direct workers resolving to Astra or Fable, including model-neutral inheritance on the two flexible premium lanes. Grants are session-bound, exact-payload-bound, atomically consumed, and expire after ten minutes. Workflow calls fail closed on those lanes because Workflow-internal workers bypass the direct-Agent hook; users can relaunch `astrax` or `fablex` for Sol-locked Workflow orchestration.
- Extended `x`, launcher-state validation, handoff selection, transcript-model normalization, aliases, installer validation, and regression tests for Astra and active Fable routing. Synthetic tests prove exact model IDs, provider preflight, worker contracts, premium approval binding/reuse/expiry/session isolation, Workflow non-bypass, retry bounds, context settings, containment, and custom-settings precedence without launching a billed worker.
- Checksum-verified and atomically activated CLIProxyAPI `v7.2.151` during a sustained network-idle window, preserving a private `v7.2.149` rollback binary. The managed service returned healthy Claude and Codex providers, advertised both Astra and Fable 5.1, and one authorized minimal Astra request returned exact model identity `gpt-6-astra`; no delegated-agent inference test ran.
