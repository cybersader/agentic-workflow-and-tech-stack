---
title: 2026-09-06 — Worklog
description: Significant scaffold changes on 2026-09-06.
stratum: 5
status: log
date: 2026-09-06
tags:
  - log
  - worklog
  - meta
---

## Portable permissions and interactive browser: pre-rollout review

- Implemented source now includes the standard-library permission core and human-only controller, the Claude Code allowance adapter and project operator, guarded allowance admission and installer paths, the separate browser-request helper, and the persistent nine-operation browser host. The operator-facing behavior is documented in [Portable agent permissions](../../stack/01-ai-coding/portable-agent-permissions.md) and [Persistent browser bridge](../../stack/01-ai-coding/browser-automation-pilot.md).
- This remains a **pre-rollout source checkpoint**, not an activation or final verification entry. No portable allowance, browser grant, live installation, public-origin session, paid worker admission, or genuine human approval UI has been exercised by this documentation task. Exact one-use Agent and Workflow approvals remain the active default and fallback until a reviewed rollout says otherwise.
- Review found and source owners fixed an actual context-ownership bug: allowance requests had risked using the stable registered-project namespace as the grant context. The adapter now derives a narrower context for each verified top-level root session from that namespace plus the exact root transcript/session key, so separate windows cannot share an allowance merely because they use the same project.
- Independent audit identified blockers in approval-display completeness, admission-cap enforcement, route and activation binding, session discovery, cross-store accounting, and browser request/host policy edges. Component owners repaired those findings, including `/status`-derived session naming, four-fact root-session fingerprints, full original-envelope validation before charging, exact additive activation scope, explicit charged-versus-uncharged fallback behavior, controller revoke wording, and browser owner/process checks. An independent component recheck confirmed the blocking repairs; passing source suites still are not release or security certification.
- Final combined signoff, source-to-installed verification, scoped installation/activation, and a genuine human-driven approval UI remain pending. No live grant migration occurred: the earlier two-fact allowance fingerprint was source-only, and the current four-fact binding intentionally refuses incompatible grants. A plain budget reinstall intentionally deactivates portable scope; preserving or adding reviewed scope requires the explicit targeted enable path.

## Scoped installation and adapter hold

- Independent combined review found no source blockers, and the portable-permissions guide now states the same-user accident-prevention boundary directly. The source guides remain source-ready overall, not rollout-complete.
- The core/controller distribution `3bcbfb7ec4eba8f69db2e1eacd28b9e339963171c5be184010d7d9790156b772` was installed and both generated launchers were independently compared. The browser distribution `52ce18de206fdcf6c577c199815956b20d5ca8fb4df00c5cae97a0d45a244340` was installed with all three launchers verified, offline dependency scripts and browser downloads disabled, Playwright and Playwright-core 1.62.1 checked, and cached Chromium revision 1234 used. All owned targets were absent before installation, so no backups were needed.
- Installation created no SQLite store, request, grant, controller process, or browser process. The model adapter, private project registry, activation state, and installed global guidance remain unchanged.
- Final deterministic suites passed separately: core 183, adapter 61, adapter CLI 41, operator 74, legacy guard 182, installer 49, portable guard 39, real-core fixtures 36, browser bridge 32, historical browser smoke 17, proxy launchers 27, launcher locks 30, cross-provider wrapper 3, and Fable delegation policy 15. These are separate receipts; fixture decisions are not evidence of a genuine human approval UI.
- Live model-adapter rollout is held because the exact target transcript contains a matching camel-case session identifier and a distinct top-level snake-case session identifier, while the parser currently combines both fields. Background session-kind role semantics also remain unresolved; this checkpoint does not infer that background means leaf. No parser source changed, an out-of-scope metadata survey was stopped and excluded from the decision, and no transcript was modified.
- The existing session remains intact and no new wave or restart is authorized. Genuine native approval and the main interactive browser demonstration remain pending. The new knowledge-base mirrors require a scoped prepublication sync; no broad publication ran, and no Fedora Obsidian link was fabricated for the WSL-specific helper path.

## Scoped activation and partial interface demonstration

- The previous adapter hold was resolved and independently source-reviewed. Canonical transcript verification now uses only top-level camel-case `sessionId`: present values must match the verified filename, while no canonical value in the bounded prefix preserves the established filename-based behavior. Existing role checks did not change, and installed CLI `bg` remains an execution mode rather than delegated authority.
- The CLI core's repaired location helpers treat a supplied environment mapping, including an empty mapping, as authoritative; resolve `HOME` lazily; avoid ambient fallback; preserve explicit whitespace; and limit tilde expansion to supported path inputs. This isolates location selection only and is not subprocess sandboxing.
- The agent-budget adapter/operator was installed as ten source-matching modules. Exactly one canonical project is both an enabled registry entry and an activated root; registration used its exact verified current session, and no model grant was created. Installed global guidance changed only in the reviewed portable-allowance bullet, with source hash `ef47534ef48a2437afeddc30e9fdbbf599d69c51614cfcf838539a51c5b68845`. Existing sessions were not restarted or rewritten; a fresh compatible context is needed to load the guidance, and an informational peer handoff conveyed neither permission nor work authorization.
- Final pins were adapter bundle `f1da6552bdade382cc6973bd98439b0ba2fae0ae61d22516ea98144a22765cea`, operator `e3c8e3d7411f1143c66c2a43399008320074db083972d04e024fccb5ca982afb`, CLI core `9f48d44b8b373256958c840613e5c8e3dff8e7ec0ac6f6e13d3412932f16277b`, guard `63ea4a941c16ee02e87ab0c8cb242a393284edb03236dcae7546e665f1d37703`, effective policy `b1ce2a36039a9ae3e1a8d97a6d25e2e40807702147aa79c5aa4a37cbf8119390`, and manifest `2f135e8426b6effde44f6b81665998502815ab0ef3c86b7b449438fa7e24b6e1`. A private pre-change backup was retained; 218 ledger/state entries, settings bytes and modes, and the permission SQLite digest were preserved.
- Installed deterministic suites passed separately with zero failures, errors, or skips: core 183, adapter 61, adapter CLI 57, operator 81, legacy guard 182, real-core fixtures 36, portable guard 39, and installer 49. Earlier browser 32, historical smoke 17, proxy launcher 27, launcher-lock 30, cross-provider wrapper 3, and Fable delegation-policy 15 receipts remain separate. Independent Opus review covered source; Sol's installed checks are execution evidence, not another independent review.
- The installed browser request helper prepared one randomized private-loopback request for 10 minutes, 6 actions, and 6 observations. No human approval was committed; installed status later reported it expired with no active grant. The owned fixture was stopped and its endpoint was confirmed closed. No browser opened, no renewal was attempted, and no model-worker grant was created.
- Task 13 remains open only for the genuine human approval and visible observe → act → observe demonstration, plus the documented handoff limitations. The two knowledge-base site mirrors remain a scoped prepublication-sync prerequisite. Fedora Obsidian handoff remains unavailable through the WSL-specific helpers; no URI, vault registration, broad sync, or publication was fabricated.

## Approval-window diagnostics and scoped update

- A 120-minute request for four Astra workers was successfully filed, but the public controller later reported `No pending permission requests`. Exact-request investigation found that its separate 15-minute prepare-to-approval window had elapsed, with no decision and no grant. The actual human controller-command time and paste-time environment are unknown: available defaults are consistent with expiry, but they do not prove the request had already expired at the exact moment the command ran.
- Independently reviewed source repairs now distinguish the approval deadline from the requested post-approval allowance duration. The operator prints the core-returned creation and expiry timestamps, timezone-bearing deadline and request window, requested allowance duration, and actual store rather than assuming 15 minutes. An unusable deadline preserves the request ID and terms, prints `UNKNOWN`, exits 2, and directs status-by-ID recovery before any duplicate prepare.
- The controller now prints the selected store and read-only expired-undecided diagnostics. It shows at most five detailed entries from a scan of at most 200 rows, labels limited counts as “at least,” never turns expired history into approval cards, and computes liveness from time even when a stale row still says `pending`. Corrupt dead-history terms or timestamps are marked unreadable without blocking healthy pending requests; corrupt live pending validation remains fail-closed. A pre-existing failed expired-state update rollback remains unfixed, and diagnostics do not clean it up. No request TTL, grant, role, operation, or approval-policy semantics changed.
- Post-update hermetic suites passed separately: core 218, including its 35-test diagnostics subset; operator 98; CLI core 57; adapter 61; legacy guard 182; adapter-to-real-core 36; portable guard 39; installer 49; launcher locks 30; and proxy launchers 27. The diagnostics subset is included in the core total and is not double-counted. Installed pins are core distribution `b08539d8f0ef946664d17c5105a8f6d06f37c2c2fdae6869d1e633810f6a1ee3`, adapter bundle `d99a4d1e655212292e69a34dfd016b8df88c6bc534b9e641aecfaf6e046f5016`, operator `ce5dd4e58ea919e14c6811a054be45b5c41dea544d196567df76313e9697c3ae`, and unchanged CLI core `9f48d44b8b373256958c840613e5c8e3dff8e7ec0ac6f6e13d3412932f16277b`.
- The targeted activation-preserving update completed without a plain budget reinstall or reregistration. Effective policy, manifest, registry, settings, and the single previously activated canonical scope remained unchanged; no request, renewal, grant, provider call, or browser UI action occurred. The live SQLite digest `82d6cff377e0508a946a5b13cb56407f45de6b1cdb921153b5248744f038e7b2` was identical before and after, the installer never connected to it, and the 144-file one-use state tree retained digest `e3da86d18ea048e0ecc59191a617e216a935fabfb07f8fa24b37dca1241c97b7`. Core-installer backups and a task-specific private backup were verified; a concurrent external backup-ring rotation was excluded from task-managed changes, and no rollback was attempted.
- Installed synthetic outputs confirmed the separate 15-minute request and 120-minute grant clocks, selected-store reporting, dead-history corruption warnings that preserve healthy live flow, and bad-timestamp `UNKNOWN` handling without retry. These are not a real human controller invocation or native grant. The browser distribution remained unchanged and no host or controller process was left running. The next human-run controller list remains only a diagnostic until its output is reported; it is not approval. Task 13 remains open for genuine native human approval, paid-model admission, and the visible browser demonstration.

## Default-local permission discovery: Task23 source checkpoint

- Updated source guidance under the frozen mode-aware architecture. The distributable policy remains disabled, `registered-only`, with empty activation and opt-out lists; a reviewed installation may later select `default-local-root`. Default availability means an eligible verified root may **ask** for its own bounded allowance without project enrollment. It is not a machine-wide grant, task authorization, model launch, browser grant, cap increase, or override of route, provider, containment, leaf, Workflow, or loaded-session restrictions.
- Aligned the canonical global instructions, the portable launcher rider, and the project-owned delegation-advisor source around ordinary-language requests such as “Let this session use Astra for two hours.” A compatible fresh root session must restate explicit purpose, duration, total-worker and per-premium-model ceilings; use exact hook-provided, shlex-quoted `--project-root` and `--transcript` operands; and leave the human to start the existing controller and decide. The read-only guard context hook never prepares, grants, renews, or writes state. If hook context is unavailable, an unregistered `default-local-root` session still needs an exact host-provided transcript for `context`, `status`, and `request`; `/status` session ID is a fallback only for an already registered project with a recorded project directory, and normal default-local flow requires no registration. The operator should run from the current workspace. For an unmatched `default-local-root` session only, process cwd must exactly match `--project-root`, a stale or different root refuses before prepare, and current process/hook payload cwd supplies project identity rather than historical transcript cwd; registered projects retain legacy behavior.
- Recorded the frozen installer controls without running them: flagless updates preserve validated enabled/mode/activation/opt-out state; explicit enable-default, disable, registered-only selection, and opt-out add/remove switches replace the old flagless reset; targeted per-project enable remains additive and preserves mode/opt-outs. Invalid installed state and incompatible repeated state-changing options must abort before writes. Task23 remains source-only with final rollout receipts pending; source-file presence is not installation evidence.
- A separate Task21 source-only implementation checkpoint completed without installation: adapter 88 (27 new), operator 108 (10 new), portable 75 (36 new), core end-to-end 39 (3 new), and unchanged legacy 182, for 492 passing tests. Source hook wiring reused the existing working-recorder JSON composition mechanism. The run made no live model invocation and no authorization, registry, or permission-state writes, but Python imports created `__pycache__`; this is not a claim of zero filesystem writes. Installer source later completed 67 passing tests at source-hash prefix `29772fa063b3`, and core D1/D2 repairs are independently closed in source. Final verifier signoff, compiled CLI source certification, installed operator default-path parity without `--policy`, deployment receipts, and genuine human/native proof remain pending.
- Preserved the same-UID trust boundary instead of inventing an HMAC, descriptor, daemon, or false caller-proof claim. Hook arguments are discovery; the operator revalidates the transcript and the admission guard independently derives the caller. Copying another session's arguments must not permit spending its grant, while preparation itself is not misrepresented as a security principal.
- This documentation worker kept SQLite, legacy ledgers, one-use approvals, registry rows, grants, requests, receipts, and transcripts untouched. It ran no live installation, controller, browser, provider, model, database, ledger, or transcript command, and no existing session was restarted. Earlier registered-only installation hashes remain historical receipts only; genuine human-native model and browser demonstrations remain unverified.
- Browser discovery now points to the already installed `browser-bridge-request`, `browser-bridge`, and `stop-browser-bridge` interfaces while keeping browser and model permissions separate. Exact fresh-profile/origin/operation/count scope and existing human approval remain mandatory; no account credentials or blanket desktop access are implied.
- A PATH-only coverage check found `claude`; `codex`, `opencode`, `aider`, and `gemini` were not found there, which is not proof of machine-wide absence. Other shell-capable agents may use the generic core CLI/JSON and governed browser interface, but no automatic guidance loading or model-budget enforcement is claimed for an unintegrated harness. CLIProxyAPI `codex` labels remain provider-route names, not evidence of the Codex CLI.
- The canonical delegation-advisor source is inside this repository, while the currently installed global copy is a separate out-of-repository directory with different bytes. Final integration repair synchronized the runtime `.claude/skills/delegation-advisor/SKILL.md` body with the named canonical `01-kernel/skills/delegation-advisor/SKILL.md` body while preserving their different required frontmatter; the kernel before-diff was limited to the stale portable-permission section, with no unrelated body content overwritten. The 15-test Fable policy suite, including exact body parity, passed. No duplicate, symlink rewrite, or installation occurred. Fedora has no registered repository-vault Obsidian helper path, so no URI or vault registration was fabricated.

## Default-local allowance rollout: Task23 closeout

- One authorized apply exited 0 at `2026-09-06T16:49:29.664885Z`. The mode-0600 receipt below `${CLAUDE_STATE_HOME}/task24-default-local-rollout/` has SHA-256 `aa5d5a66b2f53689ea0d6f30f9eef429fe69e79bf6fd6451a0443f1395e844b9`; the actual private state-home path is intentionally omitted. A sibling private backup directory contains 28 managed preimages plus one copied preflight receipt, all 29 verified.
- Installed state is enabled with `eligibility: default-local-root` and `requiresRegisteredProject: false`. One existing activated root and zero opt-outs were preserved; the one-entry registry remained byte-identical at `3b192b65a7740d5ebc646537dcac02778577df2a9443ae3a698dbfe37c33198a`, so compatible unregistered local roots need no mass enrollment. Installed hashes are policy `447f4b0fa7ef52696123f89418cb4c441c862c2cc38259c86e7aa14db51f2482`, manifest `fa7955595f2997950690281ad5d4fe4f376df5e97e27bae3533a7b39612673b7`, global guidance `301efbf542d1bdd9853762ba86c80a514e8bf157b5db345a3afbccca8efe27d7`, advisor `4a87ec83d5cbc878e897a298e0d88b42491cb3fbde85c517003495df888f24bf`, and settings `9f92c8f90e6a9b1e26ab58ee69a3a56172c109fd955b738389ebc6b9450085ce`. Guard, operator, all ten adapter modules, and both managed Workflow sources matched reviewed source, with no unexpected extra Python module. Source policy stayed inert at `1eaf8dc9931f5fe5ddc50495d1b29cee80812c985cf7287c7a027bb3107eadd0`.
- Installed hook order is context then recorder on `SessionStart`, and existing approval handling then context on `UserPromptSubmit`. The unregistered main session actually received the `UserPromptSubmit` additional context, and its restored `SessionStart` context also reported default-local request availability; both supplied exact shell-quoted project-root and transcript operands without automatic enrollment, request, or grant. These are non-interpretive hook-delivery receipts, not evidence that a human request was interpreted or that ordinary-language activation, native controller approval, paid-worker admission, or browser operation occurred.
- Main sent factual rollout FYIs to the identified Obsidian/browser and teaching-stack peer sessions. Each handoff explicitly preserved that session's already loaded restrictions and granted no permission, task, wave, or new work.
- Independent receipts passed core 218, adapter 88, core CLI 57, operator 113, legacy guard 182, portable guard 77, core end-to-end 41, installer 68, Fable policy 15, launcher-model 30, proxy launcher 27, browser bridge 32, browser smoke 17, and wrapper 3. These overlap and are not summed. A genuine isolated-home test with a space and apostrophe in its paths verified exact generated hooks and no-`--policy` operator context/request/status from actual cwd; its pending request existed only in a synthetic store and created no grant. All blocking review findings are closed, including D1/D2 cwd transitions, more-than-250-row behavior, registered continuity, D3 fallback scope, and runtime/kernel advisor parity.
- Rollout touched the live SQLite store only with `lstat`; unchanged metadata is not row proof. It did not open, hash, copy, restore, or modify the store and wrote no ledger, transcript, request, or grant. No existing session was stopped or restarted. Two low-severity robustness items remain deferred: deleted runtime cwd can produce an uncaught fail-closed traceback before prepare, and a pre-existing regex operator-verb allowlist gap exposes no grant surface.
- Task23 installation and discovery are complete; the focused documentation checks passed. Genuine native human approval, a visible browser demonstration, and paid-model admission remain separate Task13 proof; they are not installation prerequisites. A fresh compatible launch is still needed for sessions that loaded older per-launch premium rules. A PATH-only miss for other harnesses is not machine-wide unsupported proof, and generic CLI/browser callability is not a model-enforcement claim.
